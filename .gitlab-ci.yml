stages:
  - build
  - test

build:
  image: python:3.7-alpine
  stage: build
  script:
    - echo "building..."
    - pip install -r requirements.txt

test:
  image: python:3.7-alpine
  stage: test
  script:
    - echo "testing..."
    - pip install -r requirements.txt
